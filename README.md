# go-directory-cli - command library for the edge authorizer directory.

These commands are part of the [Topaz](https://github.com/aserto-dev/topaz) CLI's commands for interacting with the directory.
